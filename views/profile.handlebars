{{#if loggedIn}}
  <div class="mb-10">
    <h3>Your Questions</h3>
    <ul id="user-created-questions" class="list-disc ml-10 italic tracking-wide">
      {{#each userQuestions}}
        <li>
          {{first_choice}} VS {{second_choice}}
        </li>
      {{/each}}
    </ul>
  </div>

  <hr />

  <form id='question-form' class='form question-form'>
    <div class='form-group'>
      <h1>You have two choices, each of which's fate lies at the hands of the "lever puller". CREATIVE ANSWERS ONLY! Remember this should invoke the user to make tough decisions.</h1>
      <br>
      <label for='firstChoice-register'>First Choice:</label>
      <input placeholder="Example 1: An entire basket of kittens" class='form-input' type='text' id='firstChoice-register' />
    </div>
    <div class='form-group'>
      <label for='secondChoice-register'>Second Choice:</label>
      <input placeholder="Example 2: Your celebrity crush" class='form-input' type='text' id='secondChoice-register' />
    </div>
    <div class='form-group'>
      <button class='btn btn-primary' type='submit'>Submit</button>
    </div>
  </form>
  <script src='/js/profile.js'></script>
{{/if}}